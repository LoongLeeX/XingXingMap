# ===========================================
# 环境配置文件示例
# ===========================================
# 请复制此文件为 .env 或 .env.local 并填入真实的值
# 
# 本地开发: .env.local
# 生产环境: 在 Vercel Dashboard 配置环境变量
# ===========================================

# -------------------------------------------
# Google Maps API 配置（必需）
# -------------------------------------------
# 获取方式：https://console.cloud.google.com/
# 1. 创建或选择项目
# 2. 启用以下 API：
#    - Maps JavaScript API
#    - Places API
#    - Geocoding API
# 3. 创建 API Key（应用限制：HTTP referrers）
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# 3D 地图 Map ID（可选，用于 3D 功能）
# 创建方式：Google Cloud Console -> Maps -> Map Management
NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID=your_map_id_here

# -------------------------------------------
# 数据库配置
# -------------------------------------------
# 本地开发（SQLite）
# DATABASE_URL="file:./prisma/dev.db"

# 生产环境（PostgreSQL - Vercel Postgres 推荐）
# DATABASE_URL="postgresql://username:password@host:5432/database?schema=public"
DATABASE_URL="file:./prisma/dev.db"

# -------------------------------------------
# 应用配置
# -------------------------------------------
# 运行环境（development | production | test）
NODE_ENV=development

# 服务器端口（本地开发）
PORT=3000

# -------------------------------------------
# 文件上传配置
# -------------------------------------------
# 上传目录（本地开发使用）
# 生产环境建议使用云存储服务（见 VERCEL_DEPLOYMENT.md）
UPLOAD_DIR=./public/uploads

# 最大文件大小（字节）5MB
MAX_FILE_SIZE=5242880

# 最大文件数量
MAX_FILES_PER_UPLOAD=10

# -------------------------------------------
# Vercel 特定配置（仅生产环境）
# -------------------------------------------
# 这些变量在 Vercel 部署时会自动注入
# VERCEL=1
# VERCEL_ENV=production
# VERCEL_URL=your-app.vercel.app
# VERCEL_GIT_COMMIT_SHA=...
